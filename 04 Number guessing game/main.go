package main

/*
Number guessing game

    Guess the Number (with High Scores)
    Task: Generate a random number. The user guesses; track attempts. Save high scores to a JSON file on disk.
    Go Concepts: math/rand, encoding/json (Marshaling/Unmarshaling), os.ReadFile / os.WriteFile.

*/
